---

supervisord_prefix: /usr
supervisord_conf_path: /etc/supervisord.conf
supervisord_conf_dir: /etc/supervisord.d
supervisord_sock_path: /var/run/supervisor/supervisor.sock
supervisord_log_dir: /var/log/supervisor
supervisord_conf_ext: ini
supervisord_service_name: supervisord
